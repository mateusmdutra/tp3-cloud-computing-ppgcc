apiVersion: v1
kind: Service
metadata:
  name: proj3-dashboard-service
spec:
  type: NodePort
  selector:
    app: proj3-dashboard-streamlit
  ports:
    - port: 80
      targetPort: 8501
      nodePort: 5231
